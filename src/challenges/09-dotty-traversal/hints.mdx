## Hint

Start by opening the safe public files and watching how the requested name is
joined to the archive path.

## Hint

The cleanup removes one obvious parent-directory sequence. It does not prove
the final path is still inside the public folder.

## Hint

Use a filename that still contains a directory climb after one cleanup pass, and
aim it at the private incident note.
