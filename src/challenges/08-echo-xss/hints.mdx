## Hint

Search for a string that looks like HTML and inspect whether the page treats it
as text or markup.

## Hint

The target contains a pretend session token and an output element reserved for
the exercise.

## Hint

Submit target-specific markup that runs in the frame and writes the token into
`#exfil`.
