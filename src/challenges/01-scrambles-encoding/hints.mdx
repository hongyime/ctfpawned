## Hint

The session value is not random. Copy it somewhere you can decode browser text.

## Hint

Try decoding the blob as base64, then inspect the JSON fields it contains.

## Hint

Change the role field to the staff value the target checks, encode the JSON
again, and paste the new value back into the portal.
