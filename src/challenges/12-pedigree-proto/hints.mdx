## Hint

The importer recursively copies keys. Some JavaScript keys are not ordinary
data fields.

## Hint

The panel check happens later, on a different object from the one you imported.

## Hint

Submit JSON that gives inherited objects a truthy verification property, then
trigger the review check.
