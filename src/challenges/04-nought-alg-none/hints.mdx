## Hint

The token has three dot-separated parts. The first two parts are readable after
base64url decoding.

## Hint

The verifier reads a field from the token header before deciding how to verify
the token.

## Hint

Create a token whose header declares no signature, whose payload says admin,
and whose final signature segment is empty.
