## Hint

The target shows the query it is about to run. Read it after a normal search.

## Hint

Your search text lands inside quotes. Try input that changes the condition
rather than matching a cat name.

## Hint

Once you can return extra public rows, use a matching-column `UNION SELECT` to
read from the private notes table.
