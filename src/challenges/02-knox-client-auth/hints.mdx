## Hint

The login page itself already knows enough to decide whether to open.

## Hint

Open the target source or console and look for the function that handles the
login form.

## Hint

Use the credentials in the script, call the gate function, or set the staff
state directly. All three prove the same client-side trust problem.
